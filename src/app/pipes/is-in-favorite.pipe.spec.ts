import { IsInFavoritePipe } from './is-in-favorite.pipe';

describe('IsInFavoritePipe', () => {
  it('create an instance', () => {
    const pipe = new IsInFavoritePipe();
    expect(pipe).toBeTruthy();
  });
});
